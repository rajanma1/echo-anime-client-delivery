# ECHO ANIME Next.js Edition

## Run

1. `npm install`
2. `npm run dev`
3. Open `http://localhost:3000`

## Notes

- Uses Next.js App Router.
- Uses `jszip` for batch asset export.
- Exports 1024x1024 PNG files and metadata JSON files.
